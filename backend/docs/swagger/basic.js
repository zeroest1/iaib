/**
 * @swagger
 * /:
 *   get:
 *     summary: Check if the server is running
 *     description: Simple endpoint to verify the server is operational
 *     responses:
 *       200:
 *         description: Server is running
 *         content:
 *           text/plain:
 *             schema:
 *               type: string
 *               example: Server is running!
 */ 